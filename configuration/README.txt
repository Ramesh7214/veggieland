#nothing to read

#Maven help

mvn dependency::tree - to check missed dependencies
mvn clean install -U - it clean and rebuild the aplication and download the dependencies
mvn clean install -DskipTests - to skip test cases (not recommended)